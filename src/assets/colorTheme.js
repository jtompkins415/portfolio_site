import * as React from 'react';
import { createTheme } from '@mui/material';

const theme = createTheme({
    palette: {
        primary: {
            main: '#31283B',
            light: '#DBFFFA',
            dark: '#62948D',
            contrast_light: '#E0BBAB',
            contrast_dark: '#946653',
        },

    },
})

export default theme;

